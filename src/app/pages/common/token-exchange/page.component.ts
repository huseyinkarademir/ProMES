import { EventEmitter } from "@angular/core"

export declare class PageComponent {
    id: number
    title: string
    filters: Array<any>
    // filterComponents: Array<FilterDefinition>

    // topBarSetProperties: EventEmitter<TopBarObjectPayload>
}